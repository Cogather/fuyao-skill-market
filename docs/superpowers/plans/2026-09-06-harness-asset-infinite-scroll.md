# Harness 资产卡片分页懒加载实施计划

1. 为资产查询增加分页请求与结果类型，并用适配器回归测试锁定四类资产的分页行为。
2. 重构资产聚合服务：原子能力透传后端分页，各来源保留独立游标并在筛选会话中统一去重切页；Extension 缓存场景后分页，并返回可靠的 `hasMore`。
3. 改造资产页状态：第一页替换、后续页去重追加、旧请求失效、加载更多失败可重试。
4. 在资产滚动容器接入仅向下触发的 rAF 节流检测，补充底部加载、完成和重试反馈及滚动性能样式。
5. 运行分页适配器测试、类型检查、构建和相关 Playwright 回归，并在浏览器中检查滚动追加与筛选复位。
